{
  "name": "@pefish/js-decorator",
  "version": "0.0.3",
  "description": "",
  "keywords": [
    "common",
    "js",
    "tool",
    "ether"
  ],
  "homepage": "https://github.com/pefish/js-decorator",
  "bugs": "https://github.com/pefish/js-decorator/issues",
  "license": "MIT",
  "author": "pefish",
  "types": "./types/index.d.ts",
  "main": "./lib/index.js",
  "engines": {
    "node": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pefish/js-decorator"
  },
  "scripts": {
    "build": "rm -rf lib && rm -rf types && tsc",
    "test": "mocha --timeout 15000 --require ts-node/register src/**/*.test.ts",
    "t": "mocha --timeout 15000 --require ts-node/register",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@pefish/js-util-time": "^0.1.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.7",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
